{
  "name": "fastcast-public-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static FastCast website with native app screenshots and verified downloads",
  "scripts": {
    "test": "node tests/bandwidth.cjs && node tests/check-site.cjs",
    "check": "node --check site.js && node --check bandwidth.js && node --check tests/check-site.cjs",
    "build": "python scripts/build-site.py"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.61.0"
  }
}
